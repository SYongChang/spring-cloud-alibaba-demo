server:
  port: 9001

spring:
  application:
    name: auth-service
  cloud:
    nacos:
      discovery:
        server-addr: 119.45.5.204:8848
        namespace: public
        group: DEFAULT_GROUP
      config:
        server-addr: 119.45.5.204:8848
        namespace: public
        group: DEFAULT_GROUP
        file-extension: yml
        shared-configs:
          - data-id: common.yml
            group: DEFAULT_GROUP
            refresh: true

